import { FC } from 'react';
import { useEffect, useState } from 'react';
import styled from './WinnersLayout.module.scss';

interface IProps {
  children: React.ReactNode;
}

const WinnersLayout: FC<IProps> = ({ children }) => {
  
  return (
    <div>

    </div>
  );
};

export { WinnersLayout };
